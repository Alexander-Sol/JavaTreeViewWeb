{
  "name": "jtv-web",
  "version": "0.1.0",
  "description": "Web-based clone of JavaTreeViewer for hierarchical clustering visualization",
  "scripts": {
    "dev": "bun run build && python3 -m http.server 3000 -d public",
    "build": "bun build src/main.ts --outdir public/dist --target browser --sourcemap=external",
    "preview": "bun run build && python3 -m http.server 3000 -d public",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/bun": "^1.2.8"
  }
}
